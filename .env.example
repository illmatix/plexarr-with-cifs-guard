# .env.example

# ====== IDs & Locale ======
PUID=1000
PGID=1000
TZ=America/Edmonton
LAN_SUBNET=10.0.0.0/24
UMASK=002

# ====== Host Paths ======
# Ensure /mnt/nas is mounted before starting Docker
CONFIG_ROOT=/opt/media-stack/config
MEDIA_ROOT=/mnt/nas
TRANSCODE_DIR=/opt/media-stack/transcode

# ====== qBittorrent Ports ======
QBIT_WEBUI_PORT=8080
QBIT_TCP_PORT=6881
QBIT_UDP_PORT=6881

# ====== App Ports (host bindings) ======
SONARR_PORT=8989
RADARR_PORT=7878
PROWLARR_PORT=9696
OVERSEERR_PORT=5055
BAZARR_PORT=6767
JACKETT_PORT=9117

# ====== Plex ======
# Only needed on first run; remove after the server is claimed.
PLEX_CLAIM=claim-CHANGE_ME

# ====== VPN / NordVPN ======
# Used by the nordvpn container (compose maps USER/PASS to these)
NORD_USER=your_nord_username
NORD_PASS=your_nord_password

# Optional OpenVPN selection + settings (match your compose keys if you template them)
VPN_COUNTRY=CA;38;US;228
VPN_GROUP=Standard VPN servers
VPN_RANDOM_TOP=10
VPN_BYPASS_NETWORKS=10.0.0.0/24;192.168.1.0/24;172.20.0.0/16;172.24.0.0/16
VPN_RECREATE_CRON=0 */6 * * *
VPN_OPENVPN_OPTS=--mute-replay-warnings --pull-filter ignore "ping-restart" --ping-exit 180
VPN_PERSIST_TUN=1
VPN_DNS=1.1.1.1,8.8.8.8

# ====== (Optional) Nord WireGuard (only if you switch to a WG image) ======
NORD_WG_PRIVATE_KEY=
NORD_WG_ADDRESS=
